import { describe, it } from 'vitest';

// Flaky spawn-based test replaced by deterministic unit coverage elsewhere (sdkServer.unit.spec.ts).
// Keeping a skipped placeholder so historical test counts remain stable short-term; will remove entirely later.
describe.skip('instructions/listScoped branch coverage (skipped)', () => {
  it('placeholder skipped', () => {
    // intentionally empty
  });
});
