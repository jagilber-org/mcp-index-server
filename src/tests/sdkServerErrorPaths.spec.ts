import { describe, it } from 'vitest';

// Flaky spawn-based error path test replaced by deterministic sdkServer.unit.spec.ts coverage.
describe.skip('sdk server error & validation paths (skipped)', () => {
  it('placeholder skipped', () => {
    // intentionally empty
  });
});
